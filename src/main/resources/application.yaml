spring:
  application:
    name: backend

  datasource:
    url: jdbc:mysql://localhost:3306/ddjewelique
    username: root
    password: Developer@9841
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE

jwt:
  secret: "thisIsASuperLongSecretKeyStringAtLeast64CharactersLong1234567890!@#$%^&*()"
